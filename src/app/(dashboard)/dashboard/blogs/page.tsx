import { BlogCard } from '@/components/modules/Blogs/BlogCard';
import React from 'react';

const page = () => {
  return (
    <div>
      <BlogCard isDashboard/>
    </div>
  );
};

export default page;